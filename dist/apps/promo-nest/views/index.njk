<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ STATIC_PREFIX }}app.css">
    <script src="{{ STATIC_PREFIX }}app.js"></script>
  </head>
  <body class="app">
    <h1 class="app__title">{{ message }} 2</h1>
    <button>Click</button>
    <img src="/img/sample.webp">
    {% for i in topics %}
    <span>{{ i.id }}</span>
    {% endfor %}
    {% include 'templates/banner.njk' %}
  </body>
</html>
